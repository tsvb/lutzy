# Starred LUT selection — 2026-08-28

This is a 79-LUT snapshot of the current LUTStudio **Starred** shelf.

- Files are copied outside the active `LUTLibrary/LUTs` scan root, so this trial set does not create duplicate Library entries.
- Layout is `<Brand>/<Source>/<filename>.cube` so same-named looks from Codex, Claude, V-Log-Alchemy, and other sources remain distinguishable.
- `selection.json` records the durable LUT record ID, transform fingerprint, original locator, Brand, and Source for each file.
- The source Library remains untouched.
- The 79 local `.cube` copies are intentionally ignored until redistribution and Git LFS policy are confirmed; this README and the identity index remain tracked.
